arr=[2,3,4,5,6]

function squareVal(arr) {
    var n = 0;

    for (var i = 0; i < array.length; i++) {
        n = arr[0]*arr[0];
        arr.push(n);
        arr.shift();
    }
    return arr;
}


function squareVal(arr)
